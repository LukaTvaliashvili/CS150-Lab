package main.lesson20231218.miniExam.version1;

public class VersionOne {

    public static void main(String[] args) {

    }

    /*
    Given a string and an int n, return a string made of n repetitions of the last n characters of the string.
    You may assume that n is between 0 and the length of the string, inclusive.
    Method name: repeatLast()
     */

    /*
    Iterate over the members of the given array. If it contains an integer value presented as parameter
    'value', square the elements before the value and leave the other elements unchanged.
    There will be only one occurrence of element 'value'.

    squareBefore({1,2,3,4,5,6,7}, 4) -> {1,4,9}

     */

    /*
   Write a Java program that takes a string as input and returns an array of two strings. The first string should
   contain characters from even indexes (considering index 0 as even) capitalized, and the second string should contain
   characters from odd indexes capitalized. There will be no non-alphabetic characters.
   Name of the method: capitalizeEvenOdd().
     */
}
